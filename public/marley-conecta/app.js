const icons={home:'<path d="m3 10 9-7 9 7v10H3Z"/><path d="M9 20v-7h6v7"/>',bag:'<path d="M4 7h16l1 14H3Z"/><path d="M8 8V6a4 4 0 0 1 8 0v2"/>',bell:'<path d="M18 8a6 6 0 1 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9"/><path d="M10 21h4"/>',mail:'<path d="M4 5h16v14H4Z"/><path d="m4 7 8 6 8-6"/>',help:'<circle cx="12" cy="12" r="9"/><path d="M9 9a3 3 0 1 1 4 3c-1 .4-1 1-1 2m0 3v.1"/>',chat:'<path d="M21 11a9 9 0 0 1-9 9H3l1-5a9 9 0 1 1 17-4Z"/><path d="M8 10h8m-8 4h5"/>',user:'<circle cx="12" cy="7" r="4"/><path d="M4 21v-3a8 8 0 0 1 16 0v3"/>',pin:'<path d="M19 9c0 5-7 12-7 12S5 14 5 9a7 7 0 0 1 14 0Z"/><circle cx="12" cy="9" r="2"/>',truck:'<path d="M1 5h13v12H1Zm13 5h5l3 4v3h-8"/><circle cx="5" cy="18" r="2"/><circle cx="18" cy="18" r="2"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',coffee:'<path d="M4 6h13v8a6 6 0 0 1-13 0Zm13 1h2a3 3 0 1 1-2 6M2 21h18M7 2v1m6-1v1"/>',repeat:'<path d="m17 2 4 4-4 4M3 11V9a3 3 0 0 1 3-3h15M7 22l-4-4 4-4m14-1v2a3 3 0 0 1-3 3H3"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 6v6l4 2"/>',check:'<path d="m5 12 4 4L19 6"/>',file:'<path d="M5 2h9l5 5v15H5Zm9 0v6h5M8 12h8m-8 4h6"/>',wrench:'<path d="M21 3a6 6 0 0 1-8 8l-8 10-3-3 10-8a6 6 0 0 1 8-8l-4 4 3 3Z"/>',leaf:'<path d="M20 3C4 2 1 12 7 17s14-1 13-14ZM4 21 16 8"/>'};
const icon=n=>`<svg viewBox="0 0 24 24" aria-hidden="true">${icons[n]||icons.coffee}</svg>`;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=n=>new Intl.NumberFormat('es-CL',{style:'currency',currency:'CLP',maximumFractionDigits:0}).format(n);
const products=[{id:1,name:'One Love',desc:'Café en grano · Tueste medio',format:'Bolsa de 1 kg',price:21990,cat:'Café',note:'Equilibrado y suave'},{id:2,name:'Lively Up!',desc:'Café en grano · Tueste oscuro',format:'Bolsa de 1 kg',price:22990,cat:'Café',note:'Intenso y con cuerpo'},{id:3,name:'Get Up, Stand Up',desc:'Café en grano · Tueste claro',format:'Bolsa de 1 kg',price:22990,cat:'Café',note:'Para una taza más ligera'},{id:4,name:'Café descafeinado',desc:'Café en grano · Selección demo',format:'Bolsa de 1 kg',price:24990,cat:'Café',note:'Alternativa sin cafeína'},{id:5,name:'Vasos para café',desc:'Insumo referencial · 8 oz',format:'Paquete de 50 unidades',price:4990,cat:'Insumos',note:'Para llevar'},{id:6,name:'Limpiador de cafetera',desc:'Insumo referencial · Uso profesional',format:'Envase de 500 g',price:12990,cat:'Limpieza',note:'Revisar manual del equipo'}];
const branches=['Café del Parque · Providencia','Café del Parque · Ñuñoa'];
let page='home',tab='catalog',branch=0,query='',category='Todos',seller=false,cart={},orderSeq=1043,ticketSeq=208;
let profile={name:'Café del Parque',contact:'Daniel',email:'compras@cafedelparque.cl',address:'Av. Providencia 1234, Providencia',channel:'Horeca',reminder:false,pushAlerts:true,emailAlerts:true,updates:true};
const orders=[{id:'MC-1042',date:'10 sep 2026',status:'En preparación',branch:0,items:[{id:1,qty:4},{id:2,qty:2}],address:profile.address,history:['Solicitud recibida','Pedido confirmado','En preparación'],delivery:'14 sep · 09:00–13:00',demo:true},{id:'MC-1038',date:'28 ago 2026',status:'Entregado',branch:0,items:[{id:1,qty:4},{id:2,qty:2}],address:profile.address,history:['Pedido confirmado','Entregado al local'],delivery:'31 ago 2026',demo:true}];
const tickets=[{id:'AT-207',equipment:'Cafetera espresso · EQ-001',type:'Consulta de mantenimiento',detail:'¿Cómo coordino la próxima mantención preventiva?',impact:'Consulta',status:'En revisión',branch:0,history:[{text:'Solicitud registrada',time:'10 sep · 10:30'},{text:'Camila coordina una respuesta con servicio técnico.',time:'10 sep · 11:00'}]}];
let messages=[{mine:false,text:'Hola, soy Camila, tu ejecutiva de ejemplo. Desde aquí puedes consultar por pedidos y condiciones comerciales.',time:'Conversación de demostración'}];
const navs=[['home','home','Inicio'],['orders','bag','Pedidos'],['help','help','Ayuda'],['seller','chat','Mi vendedor'],['account','user','Mi cuenta']];
function notify(s){let el=document.querySelector('#toast');el.textContent=s;el.style.display='block';clearTimeout(notify.timer);notify.timer=setTimeout(()=>el.style.display='none',4000)}
function reminderData(){return {avgDays:14,daysSince:12,next:'20 sep 2026',risk:'Reposición cercana',items:[{id:1,qty:4},{id:2,qty:2}],basis:'Basado en las últimas compras registradas para esta sucursal de ejemplo.'}}
function reminderActive(){const r=reminderData();return profile.reminder&&r.daysSince>=r.avgDays-3}
function reminderButton(){const active=reminderActive();return `<button class="reminder-bell ${active?'active':''}" onclick="showReminder()" aria-label="${active?'Alerta de reposición activa':'Activar recordatorio de reposición'}" title="${active?'Alerta de reposición activa':'Activar recordatorio de reposición'}">${icon('bell')}<span>${active?'Activa':'Inactiva'}</span></button>`}
function reminderPreview(){const r=reminderData();return `<section class="card reminder-card ${reminderActive()?'active':''}"><div class="card-top"><div><p class="eyebrow">Alerta de compra inteligente</p><h2>Recordatorio de reposición</h2></div>${reminderButton()}</div><p class="muted">${profile.reminder?'Tu alerta está activa. La app preparará un pedido sugerido antes de tu fecha habitual de reposición.':'Activa la campana para que la app te recuerde cuándo revisar tu compra habitual.'}</p><div class="reminder-metrics"><span><strong>${r.daysSince}</strong><small>días desde el último pedido</small></span><span><strong>${r.avgDays}</strong><small>días de frecuencia promedio</small></span><span><strong>${r.next}</strong><small>próxima reposición sugerida</small></span></div><div class="row"><span class="small">${r.basis}</span><button class="btn outline" onclick="showReminder()">Configurar alerta</button></div></section>`}
function showReminder(){const r=reminderData();openModal(modalHead('Recordatorio de reposición')+`<div class="reminder-status ${reminderActive()?'active':''}">${icon('bell')}<div><strong>${profile.reminder?'Alerta de compra inteligente activa':'Alerta de compra inteligente inactiva'}</strong><p>${profile.reminder?'La campana se muestra en rojo cuando se acerca la fecha de recompra.':'La campana se mantiene gris hasta que el cliente active el recordatorio.'}</p></div></div><div class="banner"><div>${icon('coffee')}</div><div><h3>Pedido sugerido</h3><p>La app no compra automáticamente. Prepara el carrito para que el cliente revise y confirme.</p></div></div>${r.items.map(i=>{let p=products.find(p=>p.id===i.id);return `<div class="cart-line"><div class="grow"><strong>${p.name}</strong><div class="small">${p.format} · compra habitual</div></div><span class="pill">${i.qty} un.</span><strong>${money(p.price*i.qty)}</strong></div>`}).join('')}${totals(r.items)}<div class="reminder-metrics spaced"><span><strong>${r.daysSince}</strong><small>días desde último pedido</small></span><span><strong>${r.avgDays}</strong><small>frecuencia estimada</small></span><span><strong>${r.next}</strong><small>fecha sugerida</small></span></div><div class="notification-preview"><h3>${icon('mail')} Notificaciones de demostración</h3><p><strong>Dispositivo:</strong> “Tu pedido habitual Marley Coffee está listo para revisar”.</p><p><strong>Correo a ${esc(profile.email)}:</strong> “Preparamos tu pedido sugerido según tu comportamiento de compra”.</p><p class="small">El navegador puede mostrar una notificación real si autorizas permisos. El correo queda simulado hasta conectar un backend o servicio externo.</p></div><div class="modal-actions"><button class="btn outline" onclick="toggleReminder()">${profile.reminder?'Desactivar alerta':'Activar alerta'}</button><button class="btn green" onclick="testReminder()">Probar notificación</button><button class="btn" onclick="applyReminderOrder()">Revisar pedido</button></div>`)}
function toggleReminder(){profile.reminder=!profile.reminder;notify(profile.reminder?'Recordatorio de reposición activado':'Recordatorio de reposición desactivado');render();showReminder()}
function testReminder(){if(profile.pushAlerts&&'Notification'in window){if(Notification.permission==='granted')new Notification('Marley Conecta',{body:'Tu pedido habitual está listo para revisar.',tag:'marley-reposicion'});else if(Notification.permission!=='denied')Notification.requestPermission().then(p=>{if(p==='granted')new Notification('Marley Conecta',{body:'Tu pedido habitual está listo para revisar.',tag:'marley-reposicion'});});}notify(`Notificación de prueba registrada y correo simulado para ${profile.email}`)}
function applyReminderOrder(){cart=Object.fromEntries(reminderData().items.map(i=>[i.id,i.qty]));document.querySelector('#modal').close();showCart();render()}
function go(p){page=p;seller=false;render();window.scrollTo(0,0)}
function navHTML(mobile=false){return navs.map(([p,i,t])=>`<button onclick="go('${p}')" class="${page===p&&!seller?'active':''}" ${page===p&&!seller?'aria-current="page"':''}>${icon(i)}<span>${t}</span></button>`).join('')}
function render(){document.querySelector('#app').innerHTML=`<aside class="sidebar"><div class="brand">MARLEY <span>COFFEE</span><small>CONECTA / B2B</small></div><div class="side-label">TU NEGOCIO, CONECTADO</div><nav class="nav" aria-label="Principal">${navHTML()}</nav><div class="side-bottom">Propuesta académica<br>Marley Coffee Chile<br><button onclick="toggleSeller()">${seller?'Volver al cliente':'Vista del ejecutivo · demo'}</button><p style="margin-top:15px"><a href="Prompt_Marley_Conecta.md" download>Descargar prompt del proyecto</a></p></div></aside><div class="main"><header class="topbar"><div class="location">${icon('pin')}<div><small>Sucursal de ejemplo</small><select aria-label="Cambiar sucursal" onchange="branch=Number(this.value);render()">${branches.map((b,i)=>`<option value="${i}" ${i===branch?'selected':''}>${b}</option>`).join('')}</select></div></div><div class="top-actions"><span class="demo">Prototipo · datos ficticios</span><button class="cartbtn" onclick="showCart()" aria-label="Abrir carrito">${icon('bag')}<span class="count">${Object.values(cart).reduce((a,b)=>a+b,0)}</span></button><div class="avatar" aria-label="Perfil de ejemplo">DP</div></div></header><main class="content" id="main">${seller?executive():({home:home,orders:orderPage,help:helpPage,seller:sellerPage,account:accountPage}[page]())}<footer class="footer"><span>Demostración: pedidos y mensajes no se envían a Marley Coffee.<br>Los cambios duran hasta recargar esta página.</span><span><button class="btn ghost" onclick="toggleSeller()">${seller?'Volver al cliente':'Vista ejecutiva · demo'}</button> · <a href="Prompt_Marley_Conecta.md" download>Prompt</a></span></footer></main></div><nav class="mobile-nav" aria-label="Navegación móvil">${navHTML(true)}</nav>`}
function heading(k,title,sub,action=''){return `<div class="heading"><div><p class="eyebrow">${k}</p><h1>${title}</h1><p class="muted">${sub}</p></div>${action}</div>`}
function home(){const next=orders.find(o=>o.branch===branch&&o.status!=='Entregado');const t=tickets.find(t=>t.branch===branch&&t.status!=='Resuelto');return `${heading('Tu espacio Marley',`Hola, ${esc(profile.contact)}.`, '¿Qué necesita tu negocio hoy?',`<div class="primary-actions"><button class="btn" onclick="tab='catalog';go('orders')">${icon('bag')} Hacer pedido ${icon('arrow')}</button>${reminderButton()}</div>`)}${reminderPreview()}<div class="grid spaced"><section class="card delivery"><div class="card-top"><span>${icon('truck')} &nbsp;Próxima entrega</span><span class="pill">${next?esc(next.status):'Sin pedidos activos'}</span></div><h2>${next?(next.status==='Pendiente de confirmación'?'Revisaremos tu solicitud.':'Tu café está en camino a una nueva semana.'):'Prepara tu próximo pedido.'}</h2><div class="delivery-date">${next?esc(next.delivery||'Por confirmar'):'Cuando lo necesites'}</div><p class="muted">${next?`Pedido ${next.id} · fecha de ejemplo`:'Revisa tus productos y envía una solicitud.'}</p><div class="steps">${['Recibido','Preparación','En reparto','Entregado'].map((s,i)=>`<span class="step ${next&&i<({'Pendiente de confirmación':1,'En preparación':2,'En reparto':3,'Entregado':4}[next.status]||1)?'done':''}"><i>${i<1?'✓':i+1}</i>${s}</span>`).join('')}</div><button class="btn outline" onclick="${next?`orderDetail('${next.id}')`:`tab='catalog';go('orders')`}">${next?'Ver mi pedido':'Ver catálogo'} ${icon('arrow')}</button></section><section class="card"><p class="eyebrow">Atención cercana</p><h2>Tu vendedora, a un mensaje.</h2><div class="person"><div class="avatar">CV</div><div><strong>Camila Valdés</strong><p class="muted">Ejecutiva comercial · perfil ficticio</p></div></div><p class="muted">Consultas sobre pedidos, entregas y condiciones de tu negocio.</p><p class="small">${icon('clock')} Lunes a viernes · 09:00 a 18:00</p><div class="contact-buttons"><button class="btn green" onclick="go('seller')">${icon('chat')} Escribir consulta</button></div><p class="small" style="margin-top:13px">Canal de demostración, sin envío externo.</p></section></div><div class="grid spaced"><section class="card"><div class="card-top"><h2>Tu compra habitual</h2><span class="small">Ejemplo</span></div>${products.slice(0,2).map((p,i)=>`<div class="habit"><div class="product-icon">${icon('coffee')}</div><div class="name"><strong>${p.name}</strong><p class="muted">${p.format} · ${i?2:4} unidades</p></div><strong>${money(p.price*(i?2:4))}</strong></div>`).join('')}<div class="row" style="margin-top:18px"><span class="small">Precios netos referenciales</span><button class="btn outline" onclick="repeatHabit()">${icon('repeat')} Revisar y repetir</button></div></section><section class="card"><div class="card-top"><h2>Estamos pendientes</h2>${icon('help')}</div>${t?`<span class="pill amber">${esc(t.status)}</span><h3 style="margin-top:15px">${esc(t.type)}</h3><p class="muted">Caso ${t.id} · ${esc(t.equipment)}</p><p class="muted">${esc(t.history.at(-1).text)}</p><button class="btn ghost" onclick="ticketDetail('${t.id}')">Ver seguimiento ${icon('arrow')}</button>`:`<p class="muted">No tienes solicitudes de ayuda abiertas para esta sucursal.</p><button class="btn outline" onclick="go('help')">Solicitar ayuda</button>`}</section></div><div class="section-title"><h2>Más fácil, todos los días</h2></div><div class="quick-grid"><button class="quick" onclick="tab='history';go('orders')">${icon('file')}<span><strong>Consultar mis pedidos</strong><small>Estado, detalle y recompra</small></span></button><button class="quick" onclick="go('help');newTicket()">${icon('wrench')}<span><strong>Reportar una falla</strong><small>Ayuda para tus equipos</small></span></button><button class="quick" onclick="showReminder()">${icon('bell')}<span><strong>Mis recordatorios</strong><small>Alerta y pedido sugerido</small></span></button></div>`}
function orderPage(){return heading('Compra simple','Tus pedidos, sin vueltas.','Elige productos o revisa tus solicitudes.')+`<div class="seg"><button class="${tab==='catalog'?'active':''}" onclick="tab='catalog';render()">Hacer pedido</button><button class="${tab==='history'?'active':''}" onclick="tab='history';render()">Mis pedidos</button></div>${tab==='catalog'?`<div class="toolbar"><input class="search" aria-label="Buscar productos" placeholder="Buscar café o insumos…" value="${esc(query)}" oninput="query=this.value;renderProducts()"><select aria-label="Categoría" onchange="category=this.value;renderProducts()">${['Todos','Café','Insumos','Limpieza'].map(c=>`<option ${category===c?'selected':''}>${c}</option>`).join('')}</select></div><p class="notice">Catálogo ilustrativo. Precios netos referenciales en CLP; disponibilidad y condiciones por confirmar.</p><div class="products" id="products">${productCards()}</div>`:`<section class="card">${orders.filter(o=>o.branch===branch).map(o=>`<div class="order-row"><div class="product-icon">${icon('bag')}</div><div class="grow"><strong>${o.id}</strong><p class="muted">${o.date} · ${o.items.reduce((a,i)=>a+i.qty,0)} unidades</p></div><span class="pill ${o.status==='Pendiente de confirmación'?'amber':''}">${esc(o.status)}</span><button class="btn outline" onclick="orderDetail('${o.id}')">Ver detalle</button></div>`).join('')||'<div class="empty">Aún no hay pedidos en esta sucursal. Puedes comenzar desde Hacer pedido.</div>'}</section>`}`}
function productCards(){return products.filter(p=>(category==='Todos'||p.cat===category)&&(`${p.name} ${p.desc}`).toLowerCase().includes(query.toLowerCase())).map(p=>`<article class="card product"><div class="product-icon">${icon(p.cat==='Limpieza'?'wrench':p.cat==='Insumos'?'bag':'coffee')}</div><span class="tag">${p.cat.toUpperCase()}</span><h3>${p.name}</h3><p class="muted">${p.desc}</p><p class="small">${p.format}</p><div class="price">${money(p.price)}</div><span class="small">Neto por unidad</span><button class="btn outline" onclick="add(${p.id})">+ Agregar ${cart[p.id]?`(${cart[p.id]})`:''}</button></article>`).join('')||'<div class="empty">No encontramos productos. Prueba otro nombre o categoría.</div>'}
function renderProducts(){document.querySelector('#products').innerHTML=productCards()}
function add(id){cart[id]=(cart[id]||0)+1;render();notify('Producto agregado al pedido')}
function repeatHabit(){cart={1:4,2:2};showCart();render()}
function openModal(html){const m=document.querySelector('#modal');m.innerHTML=html;if(!m.open)m.showModal()}
function modalHead(title){return `<div class="modal-head"><h2>${title}</h2><button class="close" onclick="document.querySelector('#modal').close()" aria-label="Cerrar">×</button></div>`}
function amounts(items=Object.entries(cart).map(([id,qty])=>({id:Number(id),qty}))){const net=items.reduce((s,i)=>s+products.find(p=>p.id===i.id).price*i.qty,0);return {net,tax:Math.round(net*.19),total:net+Math.round(net*.19)}}
function totals(items){const a=amounts(items);return `<div style="margin-top:20px"><div class="totalrow"><span>Subtotal neto</span><span>${money(a.net)}</span></div><div class="totalrow"><span>IVA referencial (19%)</span><span>${money(a.tax)}</span></div><div class="totalrow"><span>Despacho</span><span>Por confirmar</span></div><div class="totalrow big"><span>Total sin despacho</span><span>${money(a.total)}</span></div></div>`}
function showCart(){openModal(modalHead('Revisa tu pedido')+`<div class="flow"><span>1. Productos</span><span class="current">2. Revisión</span><span>3. Envío</span></div>${Object.keys(cart).length?Object.entries(cart).map(([id,q])=>{let p=products.find(p=>p.id===Number(id));return `<div class="cart-line"><div class="grow"><strong>${p.name}</strong><div class="small">${p.format} · ${money(p.price)} neto</div></div><div class="qty"><button onclick="changeQty(${id},-1)" aria-label="Reducir ${p.name}">−</button><span>${q}</span><button onclick="changeQty(${id},1)" aria-label="Aumentar ${p.name}">+</button></div><strong>${money(p.price*q)}</strong></div>`}).join('')+totals()+`<div class="modal-actions"><button class="btn outline" onclick="document.querySelector('#modal').close();tab='catalog';go('orders')">Seguir eligiendo</button><button class="btn" onclick="checkout()">Continuar ${icon('arrow')}</button></div>`:'<div class="empty">Tu pedido está vacío.<br>Agrega productos desde el catálogo.</div>'}`)}
function changeQty(id,d){cart[id]=Math.max(0,(cart[id]||0)+d);if(!cart[id])delete cart[id];render();showCart()}
function checkout(){if(!Object.keys(cart).length)return showCart();openModal(modalHead('Datos de entrega')+`<div class="flow"><span>1. Productos</span><span>2. Revisión</span><span class="current">3. Envío</span></div><form onsubmit="submitOrder(event)"><div class="field"><label>Sucursal</label><input disabled value="${branches[branch]}"></div><div class="field"><label for="address">Dirección de ejemplo</label><input id="address" required maxlength="180" value="${esc(branch? 'Av. Irarrázaval 2345, Ñuñoa':profile.address)}"></div><div class="banner"><div>${icon('truck')}</div><div><h3>Entrega y disponibilidad por confirmar</h3><p>La ejecutiva revisará la solicitud. No se realizará ningún cobro.</p></div></div>${totals()}<label class="check"><input type="checkbox" required>Revisé productos y cantidades de esta solicitud de prueba.</label><div class="modal-actions"><button type="button" class="btn outline" onclick="showCart()">Volver</button><button class="btn" type="submit">Enviar solicitud demo</button></div></form>`)}
function submitOrder(e){e.preventDefault();if(!Object.keys(cart).length)return;const id='MC-'+orderSeq++;orders.unshift({id,date:new Date().toLocaleDateString('es-CL'),branch,status:'Pendiente de confirmación',items:Object.entries(cart).map(([id,qty])=>({id:Number(id),qty})),address:document.querySelector('#address').value.trim(),history:['Solicitud de demostración registrada'],delivery:null,demo:true});cart={};tab='history';page='orders';render();openModal(modalHead('Solicitud registrada')+`<div class="product-icon">${icon('check')}</div><h3 style="margin-top:18px">${id}</h3><p>Tu solicitud de prueba está pendiente de confirmación.</p><p class="muted">En la aplicación real, tu ejecutiva confirmaría disponibilidad, despacho y condiciones. No se ha enviado un pedido a Marley Coffee.</p><button class="btn green" onclick="orderDetail('${id}')">Ver solicitud</button>`)}
function orderDetail(id){const o=orders.find(o=>o.id===id);openModal(modalHead(o.id)+`<span class="pill">${esc(o.status)}</span><p style="margin-top:15px">${branches[o.branch]}</p><p class="muted">${esc(o.address)}<br>Entrega: ${esc(o.delivery||'Por confirmar')}</p>${o.items.map(i=>`<div class="totalrow"><span>${products.find(p=>p.id===i.id).name} × ${i.qty}</span><strong>${money(products.find(p=>p.id===i.id).price*i.qty)}</strong></div>`).join('')}${totals(o.items)}<ul class="timeline">${o.history.map(h=>`<li>${esc(h)}</li>`).join('')}</ul><div class="modal-actions"><button class="btn outline" onclick="document.querySelector('#modal').close();go('seller')">Consultar al vendedor</button><button class="btn" onclick="cart=Object.fromEntries(orders.find(o=>o.id==='${id}').items.map(i=>[i.id,i.qty]));render();showCart()">Revisar y repetir</button></div><p class="small spaced">Datos de demostración. No se generan facturas ni pagos reales.</p>`)}
function helpPage(){return heading('Seguimos contigo','¿En qué te ayudamos?','Reporta un problema y consulta su seguimiento.',`<button class="btn" onclick="newTicket()">${icon('wrench')} Solicitar ayuda</button>`)+`<div class="banner" style="margin:0 0 24px"><div>${icon('help')}</div><div><h3>Si tu equipo detiene la operación, indícalo.</h3><p>Podrás explicar el problema y adjuntar una fotografía. El caso conservará su historial.</p></div></div><section class="card"><h2>Mis solicitudes</h2>${tickets.filter(t=>t.branch===branch).map(t=>`<div class="order-row"><div class="product-icon">${icon('wrench')}</div><div class="grow"><strong>${esc(t.type)}</strong><p class="muted">${t.id} · ${esc(t.equipment)}</p></div><span class="pill ${t.impact==='Operación detenida'?'red':'amber'}">${esc(t.status)}</span><button class="btn outline" onclick="ticketDetail('${t.id}')">Ver caso</button></div>`).join('')||'<div class="empty">Sin solicitudes para esta sucursal.</div>'}</section>`}
let photo=null;
function newTicket(){photo=null;openModal(modalHead('Solicitar ayuda técnica')+`<form onsubmit="createTicket(event)"><div class="field"><label for="equipment">Equipo de ejemplo</label><select id="equipment"><option>Cafetera espresso · EQ-00${branch+1}</option><option>Molino · MO-00${branch+1}</option><option>Otro / No sé identificarlo</option></select></div><div class="field"><label for="type">¿Qué necesitas?</label><select id="type"><option>Reportar una falla</option><option>Consulta de mantenimiento</option><option>Ayuda de preparación</option></select></div><div class="field"><label for="impact">¿Cómo afecta a tu negocio?</label><select id="impact"><option>Consulta</option><option>Funcionamiento limitado</option><option>Operación detenida</option></select></div><div class="field"><label for="detail">Cuéntanos qué sucede</label><textarea id="detail" required minlength="10" maxlength="1500" placeholder="Por ejemplo: la cafetera no calienta desde esta mañana…"></textarea></div><div class="field"><label for="photo">Fotografía opcional · JPG, PNG o WebP · máx. 3 MB</label><input id="photo" type="file" accept="image/jpeg,image/png,image/webp" onchange="previewPhoto(this)"><div id="photo-preview"></div></div><p class="small">La fotografía se mantiene solo en esta página durante la demostración. No se sube a un servidor.</p><button class="btn green" type="submit">Registrar caso de prueba</button></form>`)}
function previewPhoto(input){photo=null;const box=document.querySelector('#photo-preview');box.innerHTML='';let f=input.files[0];if(!f)return;if(f.size>3*1024*1024||!['image/jpeg','image/png','image/webp'].includes(f.type)){input.value='';box.textContent='Elige una imagen válida de hasta 3 MB.';return}const reader=new FileReader();reader.onload=()=>{photo=reader.result;box.innerHTML=`<img class="photo" src="${photo}" alt="Fotografía adjunta de ejemplo">`};reader.readAsDataURL(f)}
function createTicket(e){e.preventDefault();let id='AT-'+ticketSeq++;tickets.unshift({id,branch,equipment:document.querySelector('#equipment').value,type:document.querySelector('#type').value,impact:document.querySelector('#impact').value,detail:document.querySelector('#detail').value.trim(),photo,status:'Recibido',history:[{text:'Acuse de recepción de demostración. Pendiente de asignación a servicio técnico.',time:new Date().toLocaleString('es-CL')}]});render();ticketDetail(id);notify('Caso de prueba registrado')}
function ticketDetail(id){let t=tickets.find(t=>t.id===id);openModal(modalHead(`Caso ${id}`)+`<span class="pill amber">${esc(t.status)}</span><h3 style="margin-top:20px">${esc(t.type)}</h3><p class="muted">${esc(t.equipment)} · ${esc(t.impact)}</p><p>${esc(t.detail)}</p>${t.photo?`<img class="photo" src="${t.photo}" alt="Imagen del caso">`:''}<ul class="timeline">${t.history.map(h=>`<li>${esc(h.text)}<small>${esc(h.time)}</small></li>`).join('')}</ul><div class="banner"><div>${icon('clock')}</div><div><h3>${t.status==='Resuelto'?'Caso cerrado en la demostración':'Próxima acción'}</h3><p>${t.status==='Recibido'?'Asignar responsable y evaluar el problema. El acuse automático no equivale a una respuesta técnica.':t.status==='Resuelto'?'La resolución registrada es de prueba.':'Consultar la última actualización del responsable en el historial.'}</p></div></div>${seller?`<form onsubmit="updateTicket(event,'${id}')" class="spaced"><div class="field"><label for="case-status">Estado de demostración</label><select id="case-status">${['Recibido','En revisión','En atención','Resuelto'].map(s=>`<option ${t.status===s?'selected':''}>${s}</option>`).join('')}</select></div><div class="field"><label for="case-reply">Respuesta / próxima acción</label><textarea id="case-reply" required minlength="5" maxlength="1000"></textarea></div><button class="btn green">Registrar actualización demo</button></form>`:`<button class="btn outline spaced" onclick="document.querySelector('#modal').close();go('seller')">Consultar con mi vendedor</button>`}`)}
function updateTicket(e,id){e.preventDefault();let t=tickets.find(t=>t.id===id);t.status=document.querySelector('#case-status').value;t.history.push({text:'Camila (demo): '+document.querySelector('#case-reply').value.trim(),time:new Date().toLocaleString('es-CL')});render();ticketDetail(id)}
function sellerPage(){return heading('Una relación cercana','Conversa con tu vendedora.','Mantén tus consultas y acuerdos en un mismo lugar.')+`<div class="split"><section class="card"><p class="eyebrow">Tu contacto comercial</p><div class="person"><div class="avatar">CV</div><div><h3>Camila Valdés</h3><p class="muted">Ejecutiva · perfil ficticio</p></div></div><p>Pedidos, entregas y necesidades de tu negocio.</p><p class="muted">Lunes a viernes<br>09:00–18:00 · horario de ejemplo</p><div class="banner"><div>${icon('wrench')}</div><div><h3>¿Tu máquina tiene una falla?</h3><p>Registra un caso para conservar sus antecedentes.</p><button class="btn ghost" onclick="go('help');newTicket()">Solicitar ayuda ${icon('arrow')}</button></div></div></section><section class="card"><div class="card-top"><h2>Consultas comerciales</h2><span class="pill amber">Demo</span></div><div class="chat">${messages.map(m=>`<div class="bubble ${m.mine?'mine':''}">${esc(m.text)}<small>${esc(m.time)}</small></div>`).join('')}</div><form class="chat-form" onsubmit="sendMessage(event)"><input aria-label="Escribe una consulta de prueba" id="message" required maxlength="1000" placeholder="Escribe una consulta de prueba…"><button class="btn green" type="submit" aria-label="Registrar mensaje demo">${icon('arrow')}</button></form><p class="small" style="margin-top:10px">Los mensajes no se envían. Puedes responder desde la vista ejecutiva de demostración.</p></section></div>`}
function sendMessage(e){e.preventDefault();const v=document.querySelector('#message').value.trim();if(!v)return;messages.push({mine:!seller,text:v,time:seller?'Camila · respuesta de demostración':'Registrado solo en esta demostración'});render();notify('Mensaje de prueba registrado')}
function accountPage(){return heading('Tu negocio','Mi cuenta','Personaliza los datos de ejemplo de esta sesión.')+`<div class="split"><section class="card"><h2>Datos del negocio</h2><form onsubmit="saveProfile(event)"><div class="field"><label for="business">Nombre del negocio</label><input id="business" required maxlength="80" value="${esc(profile.name)}"></div><div class="field"><label for="contact">Nombre de contacto</label><input id="contact" required maxlength="50" value="${esc(profile.contact)}"></div><div class="field"><label for="email">Correo para alertas</label><input id="email" type="email" required maxlength="120" value="${esc(profile.email)}"></div><div class="field"><label for="profile-address">Dirección principal de ejemplo</label><input id="profile-address" required maxlength="180" value="${esc(profile.address)}"></div><div class="field"><label for="channel">Canal</label><select id="channel">${['Horeca','OCS','Conveniencia','Panaderías'].map(c=>`<option ${profile.channel===c?'selected':''}>${c}</option>`).join('')}</select></div><button class="btn green">Guardar en esta sesión</button></form></section><section class="card"><h2>Mis preferencias</h2><p class="muted">El cliente decide si quiere activar el recordatorio y por qué canales recibirlo.</p><label class="check"><input type="checkbox" ${profile.reminder?'checked':''} onchange="profile.reminder=this.checked;render();notify('Recordatorio actualizado')">Recordatorio de reposición</label><label class="check"><input type="checkbox" ${profile.pushAlerts?'checked':''} onchange="profile.pushAlerts=this.checked;notify('Preferencia de dispositivo guardada')">Enviar notificación al dispositivo</label><label class="check"><input type="checkbox" ${profile.emailAlerts?'checked':''} onchange="profile.emailAlerts=this.checked;notify('Preferencia de correo guardada')">Enviar correo automático</label><label class="check"><input type="checkbox" ${profile.updates?'checked':''} onchange="profile.updates=this.checked;notify('Preferencia guardada en esta sesión')">Avisarme sobre pedidos y casos</label><div class="banner"><div>${icon('bell')}</div><div><h3>Alerta de compra inteligente</h3><p>Cuando se acerque la frecuencia habitual de compra, la campana se pondrá roja y preparará un pedido sugerido.</p><button class="btn ghost" onclick="showReminder()">Ver configuración ${icon('arrow')}</button></div></div><p class="small spaced">El envío real de correos requiere backend o un servicio externo. Esta versión muestra el flujo y puede usar la notificación del navegador si el usuario autoriza permisos.</p></section></div>`}
function saveProfile(e){e.preventDefault();profile={...profile,name:document.querySelector('#business').value.trim(),contact:document.querySelector('#contact').value.trim(),email:document.querySelector('#email').value.trim(),address:document.querySelector('#profile-address').value.trim(),channel:document.querySelector('#channel').value};render();notify('Datos de ejemplo actualizados en esta sesión')}
function toggleSeller(){seller=!seller;render();window.scrollTo(0,0)}
function executive(){return heading('Vista ejecutiva · demostración','Prioriza la atención.','Los cambios aquí se reflejan en la vista cliente de esta misma sesión.')+`<div class="quick-grid"><div class="card"><p class="muted">Solicitudes por confirmar</p><div class="metric">${orders.filter(o=>o.status==='Pendiente de confirmación').length}</div></div><div class="card"><p class="muted">Casos abiertos</p><div class="metric">${tickets.filter(t=>t.status!=='Resuelto').length}</div></div><div class="card"><p class="muted">Cuenta de ejemplo</p><strong>${esc(profile.name)}</strong><p class="small">${esc(profile.channel)} · 2 sucursales</p></div></div><div class="grid spaced"><section class="card"><h2>Atención pendiente</h2>${tickets.map(t=>`<div class="order-row"><div class="grow"><strong>${t.id} · ${esc(t.type)}</strong><p class="muted">${branches[t.branch]}<br>${esc(t.impact)}</p></div><button class="btn outline" onclick="ticketDetail('${t.id}')">Gestionar</button></div>`).join('')}</section><section class="card"><h2>Próximas acciones</h2><div class="banner"><div>${icon('clock')}</div><div><h3>Revisar los casos antes de ofrecer promociones.</h3><p>Una incidencia pendiente requiere seguimiento humano. No representa un riesgo de abandono confirmado.</p></div></div><h3 class="spaced">Responder consulta demo</h3><div class="chat">${messages.map(m=>`<div class="bubble ${m.mine?'':'mine'}">${esc(m.text)}<small>${esc(m.time)}</small></div>`).join('')}</div><form class="chat-form" onsubmit="sendMessage(event)"><input id="message" aria-label="Respuesta del ejecutivo" required maxlength="1000" placeholder="Respuesta de prueba…"><button class="btn green">${icon('arrow')}</button></form></section></div><section class="card spaced"><h2>Solicitudes de pedido</h2>${orders.map(o=>`<div class="order-row"><div class="grow"><strong>${o.id}</strong><p class="muted">${branches[o.branch]} · ${esc(o.status)}</p></div><button class="btn outline" onclick="orderDetail('${o.id}')">Ver detalle</button></div>`).join('')}</section>`}
if(document.modelContext?.registerTool){try{Promise.resolve(document.modelContext.registerTool({name:'read_demo_orders',description:'Consultar las solicitudes de demostración de esta página; no son pedidos reales.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:()=>orders.map(o=>({id:o.id,status:o.status,branch:branches[o.branch]}))})).catch(()=>{});}catch{}}
render();
